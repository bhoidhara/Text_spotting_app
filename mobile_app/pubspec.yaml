name: visiontext_mobile
version: 1.0.0+1
description: VisionText mobile client
publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_picker: ^1.0.7
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
